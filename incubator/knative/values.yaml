## Install Default RBAC roles and bindings
rbac: true

build:
  install: true
eventing:
  install: true
serving:
  install: true
  version: v0.1.1
  lite: false #For minikube users
istio:
  install: true
  version: 0.8.0
  service:
    serviceType: LoadBalancer
